@model Vidly.ViewModels.MovieFormViewModel

@{
    ViewBag.Title = "New";
    Layout = "_Layout";
}

<h2 class="h2 mb-4">
    @(Model.Movie?.Id == 0 ? "New" : "Edit") Movie
</h2>

<div style="max-width: 600px">
    @using (Html.BeginForm("Save", "Movies"))
    {
        <div class="form-group">
            @Html.LabelFor(m => m.Movie.Name)
            @Html.TextBoxFor(m => m.Movie.Name, new { @class = "form-control" })
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Movie.ReleaseDate, "Release date")
            @Html.TextBoxFor(m => m.Movie.ReleaseDate, "{0:d MMM yyyy}", new { @class = "form-control" })
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Movie.GenreId, "Genre")
            @Html.DropDownListFor(m => m.Movie.GenreId, new SelectList(Model.MovieGenres, "Id", "Name"), "Select Movie Genre", new { @class = "form-control" })
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Movie.NumberInStock, "Number in stock")
            @Html.TextBoxFor(m => m.Movie.NumberInStock, new { @class = "form-control" })
        </div>
        @Html.HiddenFor(m => m.Movie.Id)
        <div class="text-right pt-3">
            <button type="submit" class="btn btn-primary px-4">Save</button>
        </div>
    }
</div>